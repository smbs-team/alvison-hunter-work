/**
 * An interface to setup the bg colors for the jumbotron columns
 */

export interface JumbotronBgColors {
    leftColCssRules?: string,
    rightColCssRules?: string,
    wrapperBgColor?: string,
    strJumboText?: string,
    gridColumns?: string,
    childComp?: React.ReactNode,
}

